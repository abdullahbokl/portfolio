import 'dart:ui';

abstract final class AppColors {
  // ── Backgrounds ──────────────────────────────
  static const surfacePrimary = Color(0xFF0A0E1A);
  static const surfaceSecondary = Color(0xFF0F1629);
  static const surfaceTertiary = Color(0xFF1A1F35);
  static const surfaceQuaternary = Color(0xFF2A3050);

  // ── Text ─────────────────────────────────────
  static const textPrimary = Color(0xFFE2E8F0);
  static const textSecondary = Color(0xFF94A3B8);
  static const textTertiary = Color(0xFF64748B);

  // ── Accent: Neon Green ───────────────────────
  static const accentGreen = Color(0xFF39FF14);
  static const accentGreenLight = Color(0xFF00FF41);
  static const accentGreenDim = Color(0xFF1A3A1A);

  // ── Accent: Violet ───────────────────────────
  static const accentViolet = Color(0xFF8B5CF6);
  static const accentVioletLight = Color(0xFFA855F7);
  static const accentVioletDim = Color(0xFF2D1B69);

  // ── Utility ──────────────────────────────────
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF22C55E);

  // ── Terminal ─────────────────────────────────
  static const terminalGreen = Color(0xFF00FF41);
  static const terminalBg = Color(0xFF0A0A0A);
}

