name: portfolio
description: "Portfolio of Eng. Abdullah Khaled Elbokl — Mobile Application Developer."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  google_fonts: ^6.0.0
  url_launcher: ^6.3.0
  visibility_detector: ^0.4.0+2
  font_awesome_flutter: ^10.8.0
  cached_network_image: ^3.4.1
  smooth_page_indicator: ^2.0.1
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/pdf/
